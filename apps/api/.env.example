DATABASE_URL=postgresql+asyncpg://landstack:landstack@localhost:5432/landstack
# Neon: postgresql+asyncpg://user:pass@host/db?ssl=require
AUTH_MODE=dev
FIREBASE_PROJECT_ID=
GOOGLE_APPLICATION_CREDENTIALS=
CORS_ORIGINS=http://localhost:5173
DEPT_BASE_URL=
DEPT_TIMEOUT_S=0.3
EVENTS_SHARED_SECRET=change-me
REPORT_HMAC_SECRET=change-me
PUBLIC_WEB_URL=http://localhost:5173
STORAGE_BACKEND=local
STORAGE_LOCAL_DIR=./data/storage
GCS_BUCKET=
S2_OFFLINE=1
S2_DATA_DIR=./data/s2
# NVIDIA Build key (build.nvidia.com → Get API Key). Powers AI parcel briefs, officer
# advice and document extraction. Without it the AI falls back to the rule engine.
NVIDIA_API_KEY=
# NVIDIA_MODEL=nvidia/nemotron-3-super-120b-a12b   # must be invokable on YOUR key: probe GET /v1/models + a test completion (NVIDIA retires models / accounts differ)
GEMINI_API_KEY=

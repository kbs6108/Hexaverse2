import { verifyReport } from "@/lib/api";
export default async function VerifyPage({ params }: { params: Promise<{ reportId: string }> }) { const { reportId } = await params; const result = await verifyReport(reportId); return <main className="flex min-h-screen items-center justify-center bg-[#08100C] px-6 text-white"><div className="rounded-3xl border border-white/10 bg-white/[0.04] p-8 text-center"><p className="text-xs uppercase tracking-widest text-emerald-300">Public verification</p><h1 className="mt-4 text-4xl font-semibold">Report {result.valid ? "verified" : "not verified"}</h1><p className="mt-3 text-white/60">Reference: {reportId}</p></div></main>; }

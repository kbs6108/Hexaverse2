"use client";
import { useEffect, useState } from "react";
import { AppShell } from "@/components/app-shell";
import { getQueue, getStats, transitionApplication } from "@/lib/api";
import type { Application } from "@/lib/types";
export default function OfficerPage() { const [stats, setStats] = useState<Record<string, number>>({}); const [queue, setQueue] = useState<Application[]>([]); useEffect(() => { void Promise.all([getStats().then(setStats), getQueue().then(setQueue)]); }, []); return <AppShell><p className="text-xs uppercase tracking-widest text-emerald-300">Officer console</p><h1 className="mt-3 text-5xl font-semibold">Work queue</h1><div className="mt-8 grid gap-4 sm:grid-cols-3">{Object.entries(stats).map(([key, value]) => <div key={key} className="rounded-2xl border border-white/10 bg-white/[0.04] p-5"><p className="text-xs uppercase tracking-widest text-white/50">{key}</p><p className="mt-3 text-4xl font-semibold">{value}</p></div>)}</div><div className="mt-8 rounded-3xl border border-white/10 bg-white/[0.04] p-6"><h2 className="text-xl font-semibold">Department applications</h2>{queue.map((item) => <div key={item.id} className="mt-4 flex flex-wrap items-center justify-between gap-3 rounded-2xl border border-white/10 p-4"><div><p className="font-semibold">{item.id} · {item.type}</p><p className="text-xs text-white/50">{item.ulpin} · {item.status}</p></div><button type="button" onClick={async () => { await transitionApplication(item.id, "document_check", "Reviewed in demo mode"); }} className="rounded-full bg-white px-4 py-2 text-xs font-semibold text-black">Advance workflow</button></div>)}</div></AppShell>; }

"use client";
import { useState } from "react";
import { AppShell } from "@/components/app-shell";
import { createApplication, verifyOwnership } from "@/lib/api";
export default function CitizenPage() { const [result, setResult] = useState<string>(""); const [ulpin, setUlpIn] = useState("ULPIN-1403-PB-889"); const [name, setName] = useState("Ravi Kumar"); const card = "rounded-3xl border border-white/10 bg-white/[0.04] p-6"; return <AppShell><p className="text-xs uppercase tracking-widest text-emerald-300">Citizen portal</p><h1 className="mt-3 text-5xl font-semibold">Your land, clearly understood.</h1><div className="mt-10 grid gap-6 md:grid-cols-2"><form className={card} onSubmit={async (event) => { event.preventDefault(); const response = await verifyOwnership(ulpin, name); setResult(response.match ? "Match found in the mock RoR record." : "No match found."); }}><h2 className="text-xl font-semibold">Verify ownership</h2><input value={ulpin} onChange={(event) => setUlpIn(event.target.value)} className="mt-5 w-full rounded-xl border border-white/10 bg-black/20 p-3 text-sm outline-none" placeholder="ULPIN" /><input value={name} onChange={(event) => setName(event.target.value)} className="mt-3 w-full rounded-xl border border-white/10 bg-black/20 p-3 text-sm outline-none" placeholder="Claimed name" /><button className="mt-4 rounded-full bg-white px-5 py-3 text-sm font-semibold text-black">Verify Ownership</button>{result && <p className="mt-4 text-sm text-emerald-300">{result}</p>}</form><div className={card}><h2 className="text-xl font-semibold">Request a service</h2><p className="mt-2 text-sm text-white/60">Start a typed application placeholder for mutation, building permission, ownership verification, or field review.</p><button type="button" onClick={async () => { await createApplication({ type: "mutation", ulpin }); setResult("Application APP-DEMO submitted in mock mode."); }} className="mt-6 rounded-full border border-white/20 px-5 py-3 text-sm hover:bg-white/10">Create mutation request</button><p className="mt-6 text-xs text-white/40">Track Applications · APP-2048 · document_check</p></div></div></AppShell>; }

"use client";
import { useEffect, useState } from "react";
import { AppShell } from "@/components/app-shell";
import { getConnectors, grantConsent, resetDemo, simulateDeed } from "@/lib/api";
import type { Connector } from "@/lib/types";
export default function AdminPage() { const [connectors, setConnectors] = useState<Connector[]>([]); useEffect(() => { void getConnectors().then(setConnectors); }, []); return <AppShell><p className="text-xs uppercase tracking-widest text-emerald-300">Admin console</p><h1 className="mt-3 text-5xl font-semibold">Integration control room</h1><div className="mt-8 grid gap-6 md:grid-cols-2"><div className="rounded-3xl border border-white/10 bg-white/[0.04] p-6"><h2 className="text-xl font-semibold">Connectors</h2>{connectors.map((connector) => <div key={connector.department} className="mt-4 flex justify-between text-sm"><span>{connector.department}</span><span className={connector.status === "ok" ? "text-emerald-300" : "text-amber-300"}>{connector.status} · {connector.latencyMs}ms</span></div>)}</div><div className="rounded-3xl border border-white/10 bg-white/[0.04] p-6"><h2 className="text-xl font-semibold">Demo controls</h2><button type="button" onClick={async () => { await simulateDeed("ULPIN-1403-PB-889", "Ravi Kumar"); }} className="mt-5 rounded-full border border-white/20 px-4 py-2 text-sm">Simulate Deed</button><button type="button" onClick={async () => { await grantConsent("ULPIN-1403-PB-889", "admin", "24"); }} className="ml-2 rounded-full border border-white/20 px-4 py-2 text-sm">Grant Consent</button><button type="button" onClick={async () => { if (window.confirm("Reset demo data?")) await resetDemo(); }} className="mt-4 block rounded-full bg-red-400/20 px-4 py-2 text-sm text-red-200">Reset Demo</button></div></div></AppShell>; }
